# Files inside ".jsenv" directory are dynamically generated by jsenv.
# It's a filesystem cache of dynamically generated files.
# -> no need for prettier on files generated dynamically.
/.jsenv/

# Files inside "coverage" directory are generated by "npm run test-with-coverage"
# -> no need for prettier on files generated dynamically.
/coverage/

# Files inside "dist" directory are dynamically generated by "npm run build"
# -> no need for prettier on files generated dynamically.
/dist/

# Files inside "node_modules" directory are not under our control.
# -> no need for prettier on files written by others.
/node_modules/

# jsconfig.json is generated by "npm run generate-importmap". jsconfig.json
# tells VSCode where are the files until it natively support import maps.
# -> no need for prettier on files generated dynamically.
/jsconfig.json

# package-lock.json is generate by npm during npm install
# -> no need for prettier on files generated dynamically.
/package-lock.json
